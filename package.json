{
  "name": "dispo-jack",
  "version": "1.0.0",
  "description": "A mockup dispensary app. Front end runs on React. Back end runs on Express.",
  "author": "Jack Hermanson",
  "license": "WTFPL",
  "private": false,
  "scripts": {
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run build",
    "install": "(cd client && npm install) && (cd server && npm install)",
    "start": "concurrently \"cd client && PORT=3000 npm run start\" \"cd server && PORT=5000 npm run start\"",
    "start:prod": "cd server && npm run start:prod"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jack-hermanson/dispo-jack"
  },
  "devDependencies": {},
  "dependencies": {
    "concurrently": "^6.1.0"
  }
}
